// @ts-ignore
const providerHandler = require("../providers/test-provider");

const Approve = async (contract: any, eventName:any) => {
    const {signer, provider} = providerHandler()
}

module.exports = Approve
